import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles(() => ({
  avatar: {
    width: '100%',
    height: '100%'
  },
  about: {
    marginTop: 25
  },
  socialNetwork: {
    cursor: 'pointer',
    margin: 5
  },
  linkedIn: {
    color: '#0072b1'
  }
}))

export default useStyles
